# Этап 8. Инспектирование и модификация программных модулей

**Тема проекта:** Сервис фитнес-клуба (Абонементы, тренировки и посещаемость)  
**Дата выполнения:** 24.04.2026  

---

## 1. Назначение этапа

Проверить разработанные модули на соответствие стандартам кодирования для JavaScript (ES6+) и HTML/CSS, выявить нарушения и провести модификацию кода.

---

## 2. Принятые стандарты кодирования

### JavaScript (ES6+)

| Правило | Описание |
|:---|:---|
| Отступы | 2 пробела |
| Именование | camelCase для переменных и функций, UPPER_CASE для констант |
| Строки | Одинарные кавычки |
| Точка с запятой | Обязательна |
| Комментарии | JSDoc для функций |
| Объявление | const/let вместо var |

### HTML

| Правило | Описание |
|:---|:---|
| Отступы | 2 пробела |
| Атрибуты | Двойные кавычки |
| Семантика | Использование семантических тегов (header, main, section, nav, aside) |
| Идентификаторы | Уникальные id для интерактивных элементов |

### CSS

| Правило | Описание |
|:---|:---|
| Отступы | 2 пробела |
| Именование классов | kebab-case |
| Переменные | CSS Custom Properties через :root |
| Адаптивность | Media queries для мобильных устройств |

---

## 3. Чек-лист инспектирования

| № | Критерий | Результат | Комментарий |
|:--|:---|:---|:---|
| 1 | Отступы соответствуют стандарту | ✅ Пройден | 2 пробела в JS/HTML/CSS |
| 2 | Именование переменных — camelCase | ✅ Пройден | `currentUser`, `handleLogin`, `bookTraining` |
| 3 | Константы — UPPER_CASE | ✅ Пройден | `USERS`, `TRAININGS`, `SUBSCRIPTIONS`, `BOOKINGS` |
| 4 | Длина строк не превышает лимит | ⚠️ Частично | 3 строки в `app.js` превышали 120 символов (HTML-шаблоны) |
| 5 | Все функции документированы | ✅ Пройден | JSDoc-комментарии для блоков |
| 6 | Нет неиспользуемых переменных | ✅ Пройден | Проверено вручную |
| 7 | Нет дублирования кода | ✅ Пройден | Общая логика вынесена в `trainingCard()`, `showToast()` |
| 8 | Обработка ошибок | ⚠️ Частично | Добавлены проверки на null и граничные значения |
| 9 | Семантическая разметка HTML | ✅ Пройден | aside, main, nav, div с идентификаторами |
| 10 | CSS-переменные | ✅ Пройден | Цвета, отступы, тени через :root |

---

## 4. Обнаруженные нарушения и исправления

### Нарушение 1: Отсутствие проверки на null при записи

**До:**
```javascript
function bookTraining(id) {
  const training = TRAININGS.find(t => t.id === id);
  training.currentBookings += 1;
}
```

**После:**
```javascript
function bookTraining(id) {
  const t = TRAININGS.find(x => x.id === id);
  if (!t || t.currentBookings >= t.maxCapacity) {
    showToast('Нет свободных мест', 'error');
    return;
  }
  const sub = SUBSCRIPTIONS.find(s => s.clientId === currentUser.id && s.status === 'active');
  if (!sub || sub.remainingSessions <= 0) {
    showToast('Нет активного абонемента', 'error');
    return;
  }
  // ... запись создаётся только после всех проверок
}
```

### Нарушение 2: Отсутствие валидации формы входа

**До:**
```javascript
function handleLogin() {
  const email = document.getElementById('login-email').value;
  const password = document.getElementById('login-password').value;
  const user = USERS.find(u => u.email === email && u.password === password);
}
```

**После:**
```javascript
function handleLogin() {
  const email = document.getElementById('login-email').value.trim();
  const password = document.getElementById('login-password').value;
  if (!email || !password) {
    showToast('Заполните все поля', 'error');
    return;
  }
  const user = USERS.find(u => u.email === email && u.password === password && u.role === role);
  if (!user) {
    showToast('Неверные данные для входа', 'error');
    return;
  }
  // ... вход выполняется только после валидации
}
```

### Нарушение 3: Некорректное обновление счётчика при отмене

**До:**
```javascript
function cancelBooking(trainingId) {
  const b = BOOKINGS.find(x => x.trainingId === trainingId);
  b.status = 'cancelled';
  TRAININGS.find(x => x.id === trainingId).currentBookings -= 1;
}
```

**После:**
```javascript
function cancelBooking(trainingId) {
  const b = BOOKINGS.find(x => x.trainingId === trainingId && x.clientId === currentUser.id && x.status === 'active');
  if (!b) return;
  b.status = 'cancelled';
  const t = TRAININGS.find(x => x.id === trainingId);
  if (t) t.currentBookings = Math.max(0, t.currentBookings - 1);
  const sub = SUBSCRIPTIONS.find(s => s.clientId === currentUser.id && s.status === 'active');
  if (sub) sub.remainingSessions++;
  showToast('Запись отменена', 'info');
  renderSection(currentSection);
}
```

---

## 5. Итоги инспектирования

| Метрика | Значение |
|:---|:---|
| Всего проверено файлов | 4 (index.html, index.css, data.js, app.js) |
| Критериев проверено | 10 |
| Обнаружено нарушений | 5 |
| Исправлено нарушений | 5 |
| Критических ошибок | 0 |
| Код соответствует стандартам | ✅ Да |

---

## 6. Вывод

Инспектирование выявило 5 некритичных нарушений стандартов кодирования, все они были исправлены. Код приведён в соответствие со стандартами JavaScript ES6+ и HTML/CSS. Основные улучшения: добавлена валидация входных данных, проверки на null, корректная обработка граничных значений.
