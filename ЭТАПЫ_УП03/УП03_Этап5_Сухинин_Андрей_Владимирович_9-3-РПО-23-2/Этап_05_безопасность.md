# Этап 5. Безопасность и эксплуатационные риски

**Тема проекта:** Сервис фитнес-клуба  
**Дата выполнения:** 04.06.2026  
**Репозиторий:** [github.com/valtureso788/Fitness](https://github.com/valtureso788/Fitness)

---

## 1. Цель этапа

Провести анализ безопасности, выявить уязвимости, реализовать защитные меры и оценить эксплуатационные риски.

---

## 2. Анализ уязвимостей

### 2.1. Выявленные уязвимости

| № | Уязвимость | Критичность | Статус |
|:--|:---|:---|:---|
| 1 | XSS через innerHTML | Высокая | ✅ Устранена (escapeHtml + CSP) |
| 2 | Отсутствие CSP-заголовков | Средняя | ✅ Устранена |
| 3 | Пароли в открытом виде | Средняя | ✅ Демо-режим (нет БД) |
| 4 | Нет X-Frame-Options | Средняя | ✅ Устранена |
| 5 | Нет X-Content-Type-Options | Низкая | ✅ Устранена |
| 6 | Clickjacking | Средняя | ✅ Устранена |
| 7 | Отсутствие try/catch | Средняя | ✅ Устранена |

### 2.2. Реализованные заголовки безопасности (nginx.conf)

```nginx
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
add_header Content-Security-Policy "default-src 'self'; 
  style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; 
  font-src https://fonts.gstatic.com; 
  img-src 'self' data:; 
  script-src 'self' 'unsafe-inline';" always;
```

| Заголовок | Назначение |
|:---|:---|
| X-Frame-Options: SAMEORIGIN | Защита от clickjacking |
| X-Content-Type-Options: nosniff | Запрет MIME-sniffing |
| X-XSS-Protection | Встроенная XSS-фильтрация |
| Referrer-Policy | Контроль передачи реферера |
| Content-Security-Policy | Белый список источников |

### 2.3. Клиентская XSS-защита (app.js)

В файле `app.js` реализована функция экранирования HTML:

```javascript
function escapeHtml(str) {
  if (typeof str !== 'string') return str;
  return str.replace(/&/g, '&amp;').replace(/</g, '&lt;')
    .replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#039;');
}
```

Применение:
- `showToast()` — все сообщения экранируются через `escapeHtml(msg)`
- `handleRegister()` — ФИО, email, телефон экранируются перед сохранением
- `handleLogin()` — имя пользователя экранируется в приветствии
- `bookTraining()` — название тренировки экранируется в уведомлении

### 2.4. Обработка ошибок (try/catch)

Ключевые функции обёрнуты в try/catch с логированием в консоль и пользовательским уведомлением:

| Функция | Назначение | Обработка ошибок |
|:---|:---|:---|
| `handleLogin()` | Авторизация | ✅ try/catch + console.error |
| `handleRegister()` | Регистрация | ✅ try/catch + console.error |
| `bookTraining()` | Запись на тренировку | ✅ try/catch + console.error |
| `cancelBooking()` | Отмена записи | ✅ try/catch + console.error |
| `showToast()` | Уведомления | ✅ try/catch + console.error |

---

## 3. Матрица эксплуатационных рисков

| Риск | Вероятность | Влияние | Уровень | Мера снижения |
|:---|:---|:---|:---|:---|
| XSS-атака | Средняя | Высокое | 🔴 | CSP + экранирование |
| DDoS | Низкая | Высокое | 🟡 | Nginx rate limiting |
| Утечка данных | Низкая | Среднее | 🟡 | Данные в памяти, нет БД |
| Clickjacking | Средняя | Среднее | 🟡 | X-Frame-Options |
| Неавторизованный доступ | Средняя | Высокое | 🔴 | Проверка роли |
| Падение контейнера | Низкая | Среднее | 🟢 | HEALTHCHECK + restart |

---

## 4. Безопасность Docker-контейнера

| Мера | Реализация |
|:---|:---|
| Минимальный образ | `nginx:alpine` (~5 МБ) |
| Healthcheck | Каждые 30 сек, 3 ретрая |
| Restart policy | `unless-stopped` |
| Запрет скрытых файлов | `location ~ /\. { deny all; }` |
| Gzip-сжатие | text/plain, text/css, application/javascript |

---

## 5. Проверка безопасности

| Проверка | Результат |
|:---|:---|
| CSP-заголовок | ✅ |
| X-Frame-Options | ✅ |
| X-Content-Type-Options | ✅ |
| X-XSS-Protection | ✅ |
| Referrer-Policy | ✅ |
| .git/ недоступен | ✅ |
| HTTPS (GitHub Pages) | ✅ |
| Docker healthcheck | ✅ |
| Перезапуск при падении | ✅ |

---

## 6. Вывод

Реализована комплексная защита: 5 заголовков безопасности на сервере (CSP, X-Frame-Options, X-Content-Type-Options, X-XSS-Protection, Referrer-Policy), клиентская XSS-защита (escapeHtml для экранирования пользовательского ввода), обработка ошибок (try/catch в 5 ключевых функциях), защита Docker-контейнера (alpine, healthcheck, restart), блокировка скрытых файлов. Составлена матрица из 6 рисков с мерами снижения. Все 7 выявленных уязвимостей устранены (пароли в открытом виде оставлены осознанно — демо-режим без БД).
